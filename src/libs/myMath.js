//@ts-check

/**
 * My math library
 * @module myMath
 */
//@modules we separate the libraries in modules that we can use in other projects

/**
 * Add numbers two numbers 
 * @param {Number} x first number
 * @param {Number} y second number
 * @returns { Number} add of x and y
 */
const myAdd = (x, y) => x + y
// if I put a same description as another function, In the documentation will only show one

/**
 * substract two numbers and return a Number
 * @param {Number} x  first number
 * @param {Number} y second number
 * @returns {Number} Rest of x and y
 */
const mySubstract = (x, y) => x - y